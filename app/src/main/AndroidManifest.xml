<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:label="VoteRoom"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity android:name=".ui.ResultsActivity" />
        <activity android:name=".ui.RoomManagementActivity" />
        <activity android:name=".ui.AddVotingActivity" />
        <activity android:name=".ui.CreateRoomActivity" />
        <activity android:name=".ui.JoinRoomActivity" />
        <activity android:name=".ui.ModeratorLoginActivity" />
        <activity android:name=".ui.RoomSessionActivity"/>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>